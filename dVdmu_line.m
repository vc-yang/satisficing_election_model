dVdmu = (sqrt((sigma0 ^ 2 + sigma(i) ^ 2) / sigma0 ^ 2) * exp(-(sigma0 ^ 2 * mu(i) ^ 2 - 0.2e1 * sigma0 ^ 2 * mu(i) * mu(j) + sigma0 ^ 2 * mu(j) ^ 2 + mu(i) ^ 2 * sigma(j) ^ 2 + mu(j) ^ 2 * sigma(i) ^ 2) / (sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / 0.2e1) * sigma0 ^ 4 * mu(i) * sigma(j) - sqrt((sigma0 ^ 2 + sigma(i) ^ 2) / sigma0 ^ 2) * exp(-(sigma0 ^ 2 * mu(i) ^ 2 - 0.2e1 * sigma0 ^ 2 * mu(i) * mu(j) + sigma0 ^ 2 * mu(j) ^ 2 + mu(i) ^ 2 * sigma(j) ^ 2 + mu(j) ^ 2 * sigma(i) ^ 2) / (sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / 0.2e1) * sigma0 ^ 4 * mu(j) * sigma(j) + sqrt((sigma0 ^ 2 + sigma(i) ^ 2) / sigma0 ^ 2) * exp(-(sigma0 ^ 2 * mu(i) ^ 2 - 0.2e1 * sigma0 ^ 2 * mu(i) * mu(j) + sigma0 ^ 2 * mu(j) ^ 2 + mu(i) ^ 2 * sigma(j) ^ 2 + mu(j) ^ 2 * sigma(i) ^ 2) / (sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / 0.2e1) * sigma0 ^ 2 * mu(i) * sigma(i) ^ 2 * sigma(j) + sqrt((sigma0 ^ 2 + sigma(i) ^ 2) / sigma0 ^ 2) * exp(-(sigma0 ^ 2 * mu(i) ^ 2 - 0.2e1 * sigma0 ^ 2 * mu(i) * mu(j) + sigma0 ^ 2 * mu(j) ^ 2 + mu(i) ^ 2 * sigma(j) ^ 2 + mu(j) ^ 2 * sigma(i) ^ 2) / (sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / 0.2e1) * sigma0 ^ 2 * mu(i) * sigma(j) ^ 3 - sqrt((sigma0 ^ 2 + sigma(i) ^ 2) / sigma0 ^ 2) * exp(-(sigma0 ^ 2 * mu(i) ^ 2 - 0.2e1 * sigma0 ^ 2 * mu(i) * mu(j) + sigma0 ^ 2 * mu(j) ^ 2 + mu(i) ^ 2 * sigma(j) ^ 2 + mu(j) ^ 2 * sigma(i) ^ 2) / (sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / 0.2e1) * sigma0 ^ 2 * mu(j) * sigma(i) ^ 2 * sigma(j) + sqrt((sigma0 ^ 2 + sigma(i) ^ 2) / sigma0 ^ 2) * exp(-(sigma0 ^ 2 * mu(i) ^ 2 - 0.2e1 * sigma0 ^ 2 * mu(i) * mu(j) + sigma0 ^ 2 * mu(j) ^ 2 + mu(i) ^ 2 * sigma(j) ^ 2 + mu(j) ^ 2 * sigma(i) ^ 2) / (sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / 0.2e1) * mu(i) * sigma(i) ^ 2 * sigma(j) ^ 3 - 0.2e1 * sqrt((sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / sigma0 ^ 2) * exp(-mu(i) ^ 2 / (sigma0 ^ 2 + sigma(i) ^ 2) / 0.2e1) * sigma0 ^ 2 * mu(i) * sigma(i) ^ 2 - 0.2e1 * sqrt((sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / sigma0 ^ 2) * exp(-mu(i) ^ 2 / (sigma0 ^ 2 + sigma(i) ^ 2) / 0.2e1) * sigma0 ^ 2 * mu(i) * sigma(j) ^ 2 - 0.2e1 * sqrt((sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / sigma0 ^ 2) * exp(-mu(i) ^ 2 / (sigma0 ^ 2 + sigma(i) ^ 2) / 0.2e1) * mu(i) * sigma(i) ^ 2 * sigma(j) ^ 2) * sigma(i) * ((sigma0 ^ 2 + sigma(i) ^ 2) / sigma0 ^ 2) ^ (-0.1e1 / 0.2e1) * ((sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / sigma0 ^ 2) ^ (-0.1e1 / 0.2e1) / (sigma0 ^ 2 + sigma(i) ^ 2) / (sigma(i) ^ 2 * sigma0 ^ 2 + sigma(j) ^ 2 * sigma0 ^ 2 + sigma(i) ^ 2 * sigma(j) ^ 2) / sigma0 / 0.2e1;
